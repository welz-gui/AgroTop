"""Camada de apresentação do AgroTop.

Por ora contém apenas `tema.py` — a paleta única (ROADMAP.md R20, DESIGN.md).

As páginas vivem em `app.py` e migrarão para `views/` — **nunca** para `pages/`,
que é nome reservado pela convenção de multipage do Streamlit e criaria navegação
automática, contornando o guard de perfil do `main()` (ROADMAP.md R12).
"""
